module.exports = {
  // Print semicolons at the ends of statements.
  semi: false,
  trailingComma: 'es5',
  singleQuote: true,
  // length line width
  printWidth: 80,
  tabWidth: 2,
  endOfLine: 'auto',
  // "avoid" = x => x, "always" =(x)=x
  arrowParens: 'avoid',
  // true - Example: { foo: bar }
  //   bracketSpacing: true,
}
// prettier override eslint if config rules happen
